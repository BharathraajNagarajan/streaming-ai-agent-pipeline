# architecture/

Three Mermaid diagrams (rendered natively by GitHub — no image files needed):

- **`system-architecture.md`** — the full pipeline, producer to AI answer
- **`medallion-flow.md`** — what specifically happens at each of Bronze/Silver/Gold
- **`agent-flow.md`** — how the Cortex Agent is grounded through the semantic view

These are referenced from the root `README.md`; this folder exists so each
diagram also has room for its own explanation without cluttering the main
README.
